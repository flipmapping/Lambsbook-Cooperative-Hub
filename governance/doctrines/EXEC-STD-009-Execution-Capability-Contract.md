RMP execution SHALL consult the ECC before generating any mutation package.
EMP execution SHALL update the ECC whenever framework capabilities change.
RMP execution SHALL NOT assume capabilities absent from the ECC.
If a required capability is absent, stop RMP work and open exactly one bounded EMP.