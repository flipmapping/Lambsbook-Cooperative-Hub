# LIVE EXECUTION REQUIRED

The evidence directory contains placeholder files.

No additional governance artifacts should be created.

Replace the placeholders by performing ONE real execution.

Required evidence:

1. http-request.txt
   Paste the actual HTTP request (or describe the exact UI action).

2. http-response.json
   Paste the actual API response.

3. server.log
   Paste the relevant server log generated by that request.

4. notification-record.json
   Paste the persisted notification record (or equivalent database evidence).

5. verification-notes.md
   Record:
   - environment
   - timestamp
   - outcome
   - exactly-once observation
   - unexpected errors (if any)

After all five artifacts contain genuine runtime evidence,
submit them together for Growth Engine review.

Do not create additional GEX-EXEC packages before this step is complete.
